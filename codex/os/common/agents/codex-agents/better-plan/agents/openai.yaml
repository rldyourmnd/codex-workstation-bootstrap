interface:
  display_name: "Better Plan"
  short_description: "Tactical implementation planning"
  default_prompt: "Use $better-plan to create a step-by-step implementation plan with validation, dependencies, and rollback criteria."

dependencies:
  tools:
    - type: "mcp"
      value: "sequential-thinking"
      description: "Plan decomposition and sequencing"
    - type: "mcp"
      value: "serena"
      description: "Codebase-aware scoping"

policy:
  allow_implicit_invocation: true
