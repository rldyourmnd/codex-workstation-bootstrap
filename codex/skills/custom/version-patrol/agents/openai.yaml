interface:
  display_name: "Version Patrol"
  short_description: "Dependency and runtime freshness audit"
  default_prompt: "Use $version-patrol to audit dependencies and runtimes, then return a severity-ranked upgrade plan with migration notes."

dependencies:
  tools:
    - type: "mcp"
      value: "context7"
      description: "Official docs and migration references"

policy:
  allow_implicit_invocation: true
