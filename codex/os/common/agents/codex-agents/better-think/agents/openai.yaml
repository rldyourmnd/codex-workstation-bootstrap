interface:
  display_name: "Better Think"
  short_description: "Deep architecture reasoning and synthesis"
  default_prompt: "Use $better-think to compare solution options, run structured reasoning, and return a decision memo with explicit tradeoffs."

dependencies:
  tools:
    - type: "mcp"
      value: "sequential-thinking"
      description: "Structured multi-round reasoning"
    - type: "mcp"
      value: "context7"
      description: "Verification against official docs"
    - type: "mcp"
      value: "serena"
      description: "Memory and symbol context for repository-grounded decisions"

policy:
  allow_implicit_invocation: true
