interface:
  display_name: "Better Debugger"
  short_description: "Evidence-backed root-cause debugging"
  default_prompt: "Use $better-debugger to trace this failure from symptoms to root cause and return an evidence-backed fix validation plan."

dependencies:
  tools:
    - type: "mcp"
      value: "serena"
      description: "Symbol-level code tracing"

policy:
  allow_implicit_invocation: true
