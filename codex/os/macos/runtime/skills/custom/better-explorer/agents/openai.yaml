interface:
  display_name: "Better Explorer"
  short_description: "Semantic repository investigation and tracing"
  default_prompt: "Use $better-explorer to trace this code path end-to-end and return evidence-backed findings with file references."

dependencies:
  tools:
    - type: "mcp"
      value: "serena"
      description: "Semantic symbol analysis and reference tracing"
    - type: "mcp"
      value: "sequential-thinking"
      description: "Reasoning synthesis for complex investigations"

policy:
  allow_implicit_invocation: true
