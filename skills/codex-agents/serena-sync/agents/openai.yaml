interface:
  display_name: "Serena Sync"
  short_description: "Synchronize and audit Serena memories"
  default_prompt: "Use $serena-sync to audit `.serena/memories` freshness and update stale memory files with verified facts."

dependencies:
  tools:
    - type: "mcp"
      value: "serena"
      description: "Memory operations and semantic code analysis"

policy:
  allow_implicit_invocation: true
